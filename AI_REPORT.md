# AI Report - Лабораторная работа №5

**Студент:** Яппаров Эмир  
**Дата:** 14 февраля 2026  
**Репозиторий:** [Lab5_frontend](https://github.com/Codekeeper45/Lab5_frontend.git)

---

## 📋 Описание проекта

Данный проект представляет собой React-приложение с использованием TypeScript для управления и поиска пользователей. Приложение демонстрирует основные концепции современной веб-разработки, включая типизацию, компонентную архитектуру и управление состоянием.

---

## 🛠️ Используемые технологии

- **React 19.2.0** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный язык программирования
- **Vite** - современный инструмент сборки
- **ESLint** - линтер для проверки качества кода

---

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── UserCard.tsx    # Карточка пользователя
│   └── SkillList.tsx   # Список навыков с цветовой индикацией
├── types/              # TypeScript типы и интерфейсы
│   └── index.ts        # Определения User, Skill, SkillLevel
├── SearchApp.tsx       # Главный компонент приложения
├── main.tsx            # Точка входа
└── index.css           # Глобальные стили
```

---

## 🎯 Реализованная функциональность

### 1. **Типизация (TypeScript)**
- Интерфейс `User` с полями: name, email, age, skills
- Интерфейс `Skill` с полями: id, name, level
- Тип `SkillLevel` с фиксированными значениями: "beginner" | "intermediate" | "Expert"

### 2. **Компоненты**

#### **UserCard.tsx**
- Принимает типизированные props: `user`, `isActive`, `children`
- Условное отображение стилей на основе активности пользователя
- Интеграция компонента `SkillList` для отображения навыков

#### **SkillList.tsx**
- Отображает список навыков с цветовой индикацией уровня:
  - 🟢 Expert - зеленый
  - 🟡 intermediate - желтый
  - 🔴 beginner - красный

### 3. **Управление состоянием**
- `useState<User[]>` для хранения списка пользователей
- `useState<string>` для поискового запроса
- Фильтрация пользователей в реальном времени

### 4. **Обработка событий**
- `handleSearch` - типизированный обработчик `onChange` для input
- `handleClear` - обработчик сброса фильтра

---

## ✨ Ключевые особенности кода

### Типобезопасность
```typescript
const [users] = useState<User[]>(INITIAL_USERS);
const handleSearch = (event: React.ChangeEvent<HTMLInputElement>) => {
    // ...
};
```

### Компонентная архитектура
- Разделение ответственности между компонентами
- Переиспользуемость компонентов
- Props drilling с типизацией

### Условный рендеринг
```tsx
{filteredUsers.length > 0 ? (
    // Отображение карточек
) : (
    <p>No users found.</p>
)}
```

---

## 🎨 Стилизация

- Inline-стили для динамического изменения внешнего вида
- Цветовая индикация статуса пользователя (активный/неактивный)
- Адаптивные стили для элементов формы
- Светлая тема с черным текстом на белом фоне

---

## 🚀 Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

---

## 📝 Выводы

В ходе выполнения лабораторной работы были освоены:
1. Основы TypeScript в React-приложениях
2. Создание типизированных компонентов и props
3. Работа с хуками useState и типизацией состояния
4. Организация структуры проекта по best practices
5. Работа с Git и GitHub для версионного контроля

---

## 🔗 Ссылки

- **GitHub Repository:** https://github.com/Codekeeper45/Lab5_frontend.git
- **Автор:** Яппаров Эмир
